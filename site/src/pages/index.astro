---
// spacing.purr.io — the well-spacing playbook as an essay: the
// wrong-sign trap, the natural experiment that fixes it, the model,
// and the honest end. Numbers come verbatim from reports/.
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Well Spacing Playbook</title>
    <link rel="canonical" href="https://spacing.purr.io/" />
    <meta
      name="description"
      content="Parent-child interference, measured on public Alberta data: the naive answer has the wrong sign. An ML spacing playbook for geoscientists and engineers, taken exactly as far as public data can go — and what would turn it into a well planner."
    />
  </head>
  <body>
    <nav class="topnav">
      <a class="logo" href="https://purr.io" aria-label="purr.io">
        <img src="/purrio_2026.png" alt="purr.io logo" />
      </a>
      <span class="brand">Well Spacing Playbook</span>
      <a href="https://github.com/rbhughes/well-spacing-playbook">GitHub</a>
      <span class="vintage">Alberta public data &middot; 2025</span>
    </nav>

    <header class="masthead">
      <p class="kicker">A CAUTIONARY TALE, THEN A PLAYBOOK</p>
      <h1>How close is too close?</h1>
      <p class="standfirst">
        Every operator knows parent&ndash;child interference is real:
        infill too tight and the children drain the parents&rsquo;
        rock. So why does a straightforward regression on public data
        conclude that crowded wells produce <em>more</em>? This
        project answers that with a measurement, builds a calibrated
        forecasting model on free Alberta data, and stops &mdash;
        honestly &mdash; exactly where public data runs out. Which is
        also where it gets interesting for anyone holding the
        proprietary pieces.
      </p>
    </header>

    <article class="body">
      <h2>Three numbers</h2>
      <div class="cards">
        <div class="card">
          <div class="num confound">+1.9</div>
          <p><strong>The high-grading trap.</strong> Compare wells
          <em>across</em> an asset and more neighbouring withdrawal
          correlates with <em>more</em> production. That isn&rsquo;t
          physics &mdash; it&rsquo;s development behaviour. Operators
          high-grade: the best rock gets the tightest spacing, so
          crowding becomes a proxy for rock quality. Any spacing study
          built on cross-asset comparison inherits this and reports
          interference with the wrong sign.</p>
        </div>
        <div class="card">
          <div class="num true-sign">&minus;5.3</div>
          <p><strong>Same-pad siblings tell the truth.</strong> Wells
          on one pad share rock, vintage, and completion era &mdash;
          so comparing only pad-mates removes geology from the
          question. Across 4,858 wells on 1,416 multi-well pads, the
          slope flips negative [95% CI &minus;11.4 to &minus;0.5,
          cluster-bootstrapped by pad]. That is interference,
          isolated the way a field trial would isolate it.</p>
        </div>
        <div class="card">
          <div class="num penalty">+16.7</div>
          <p><strong>The what-if price.</strong> Ask the trained model
          &ldquo;what would this well have done without its
          pad-mates?&rdquo; and the median answer is a 16.7-point
          crowding penalty on first-year pace, relative to peers
          [p25/p75 +7.3/+31.4]. Material money, right sign &mdash;
          and, on public data alone, too uncertain to set spacing
          with. The report says so in as many words.</p>
        </div>
      </div>

      <figure>
        <svg id="contrast" width="820" height="200" role="img"
          aria-label="Between-pad vs within-pad slope estimates"></svg>
        <figcaption>
          Crowding-vs-production slopes, in points of peer-median pace
          per unit of exposure, under two definitions of crowding:
          offset withdrawal (how much the neighbours produced) and
          offset count (how many neighbours). Both flip sign the
          moment geology is held fixed by comparing only same-pad
          wells; the withdrawal-weighted version excludes zero.
          Verbatim from <code>reports/within_pad_contrast.md</code>.
        </figcaption>
      </figure>

      <h2>The geometry underneath</h2>
      <p>
        Everything rests on the same kind of 3-D wellbore
        reconstruction a drilling team runs for anti-collision
        &mdash; except province-wide, from the public record. Alberta
        publishes the directional surveys operators file (AER ST37),
        and from them this pipeline rebuilds
        <strong>105,724 laterals across 65,082 horizontal wells</strong>
        deterministically: true closest approach in 3-D, TVD offset,
        lateral overlap length, and relative azimuth, computed
        well-to-well <em>and leg-to-leg within multilaterals</em>.
        That second part matters: in the Clearwater and other
        heavy-oil fairways, multi-leg fishbone wells
        drain against their own branches, and almost no public
        analysis quantifies it.
      </p>
      <p>
        Here is one of those wells, live from the data &mdash; not a
        cartoon. Nine surveyed legs of a single Baytex multilateral
        in the Peace River heavy-oil area (licence 0318429), rebuilt
        from the 1,569 survey stations in its public ST37 record: one
        build section leaving surface, then a fan of laterals landing
        in the pay and running up to 1.7&nbsp;km.
      </p>
      <figure>
        <div id="fish-box" class="fish-box">
          <canvas id="fishbone"></canvas>
        </div>
        <figcaption>
          A real nine-leg fishbone, drawn from its AER ST37
          directional surveys in true 3-D (2&times; vertical
          exaggeration; ticks drop to the landing plane).
          <strong>Drag to rotate.</strong> The question the model
          prices: how much do these legs steal from each other &mdash;
          and from the pad next door?
        </figcaption>
      </figure>
      <p>Every lateral in the province is drawn below.</p>
      <div id="map" class="map-box" role="application"
        aria-label="All Alberta horizontal well laterals"></div>
      <p class="mapnote">
        Each line is one leg, heel to toe (straightened for display).
        Zoom into the Montney or Clearwater fairways and the pad
        architecture &mdash; parallel fans of sibling laterals &mdash;
        is the between-pad vs within-pad distinction made visible.
        Two-finger scroll passes through; use &#8984;+scroll to zoom.
      </p>

      <h2>The model, in reservoir terms</h2>
      <p>
        Set aside the ML vocabulary for a moment and think about how
        an experienced engineer forecasts a new well: look at the
        offsets. Which wells are nearby, how close do they run, how
        much have they already withdrawn, how long have they been on.
        Then weigh them &mdash; a parallel lateral 150&nbsp;m away
        that has produced hard for three years counts for a lot; a
        distant toe-to-toe neighbour counts for little. The model does
        exactly this, except the weighting is <em>learned</em> from
        65,000 wells instead of hand-built.
      </p>
      <p>
        The machinery that does the weighing is a <strong>set
        encoder</strong> &mdash; and the name is less exotic than it
        sounds. An offset list has no natural order and no fixed
        length: one well has two neighbours, another has thirty. A
        set encoder is a network built to accept exactly that kind of
        input &mdash; each offset described by its geometry (closest
        approach, TVD offset, overlap, azimuth), timing, and
        withdrawal history &mdash; and compress the list into a
        summary, learning which offsets deserve attention. Think of
        it as a drainage-weighted offset summary where the weights
        came from data rather than judgment. There are two of them:
        one over the well&rsquo;s neighbours, one over its
        <em>own legs</em>, so a fishbone&rsquo;s self-interference is
        priced by the same mechanism.
      </p>
      <p>
        The output is deliberately in the industry&rsquo;s native
        uncertainty language: <strong>P10/P50/P90</strong> of
        first-year production pace, expressed relative to peer wells
        in the same play and vintage. Not a single-number type-curve
        multiplier &mdash; a band. And a band is only useful if
        it&rsquo;s honest: on wells the model never saw, actuals fell
        below its P10 12% of the time, below its P50 53%, below its
        P90 88% &mdash; against the nominal 10/50/90. That&rsquo;s
        the property reserves bands are supposed to have and often
        don&rsquo;t.
      </p>

      <h2>What survived the gate</h2>
      <p>
        Before any of its output counts, the model had to pass a
        blind lookback &mdash; the ML term is a <em>temporal
        holdout</em>. The newest wells were sealed off during all
        development and opened <strong>exactly once</strong>, the
        way you&rsquo;d grade last year&rsquo;s forecast against this
        year&rsquo;s actuals: no peeking, no second attempt, and a
        pre-registered rule that if the model lost to simpler
        methods, nothing downstream ships. It won &mdash; median
        forecast error of <strong>71.6 points vs 78.1</strong> for
        gradient boosting, the strongest of the conventional
        tabular-ML baselines &mdash; with the calibration above.
      </p>
      <p>
        It also had to behave like a reservoir, not a curve-fit.
        The counterfactual machinery works like a what-if run:
        re-forecast the same well with its offsets&rsquo; withdrawal
        history erased, and the difference is the interference price.
        That price is near zero for isolated wells and grows
        monotonically with crowding &mdash; a hard physics gate the
        model passed.
      </p>
      <figure>
        <svg id="penalty" width="700" height="220" role="img"
          aria-label="Median context penalty by neighbour count"></svg>
        <figcaption>
          Median depletion penalty by offset count: the drop in
          forecast pace when the what-if erases the offsets&rsquo;
          withdrawal history &mdash; from
          <code>reports/counterfactual_summary.md</code>. Monotone in
          crowding, near zero for isolated wells: the model behaves
          like drainage, not like a curve-fit.
        </figcaption>
      </figure>
      <p>
        And what <em>failed</em> is printed, not hidden. One probe
        walks a hypothetical offset in from 1,500&nbsp;m to
        100&nbsp;m with <em>no</em> production history &mdash; an
        undrilled permit next door, in effect &mdash; and the
        forecast barely moves. Reservoir intuition says it
        shouldn&rsquo;t move much (an undrained neighbour
        hasn&rsquo;t taken your oil yet), but it also flags what the
        model can&rsquo;t see: frac-hit risk from a <em>future</em>
        completion nearby, because public Alberta data carries no
        completion design at all. The model learned interference
        through <em>depletion</em> &mdash; the channel the data
        actually identifies &mdash; not through bare distance, and
        the evaluation gate records <code>approach&nbsp;FAIL</code>
        in the same table as its passes.
      </p>

      <h2>The playbook, as it was actually built</h2>
      <p>
        The repo is organized as the sequence it was developed in
        &mdash; each phase a runnable stage with its own artifact,
        so a team with proprietary data can replay it with their
        inputs:
      </p>
      <ol class="phases">
        <li><strong>0&ndash;2 · Cohort &amp; geometry.</strong>
          Parse the ST37 shapefiles with a purpose-built PolyLineZ
          reader (no GDAL &mdash; the format is simple enough to
          read raw, and the pipeline stays dependency-light), keeping
          only bores flagged <code>Surveyed</code>, since the
          <code>Calculated</code> ones are two-vertex sticks with no
          real survey. Join geometry to Petrinex on the UWI built
          from the DLS label &mdash; where the details bite: the
          location exception is <em>alphanumeric</em> (F1, AA,
          W0&hellip;), and a digits-only parse silently dropped
          11.7% of the province before a reconciliation check that
          refuses to lump &ldquo;excluded&rdquo; with
          &ldquo;unexplained&rdquo; caught it. Final parse: 532,553
          of 532,623 wells (99.99%). Distances are computed in a
          local metre frame, never Web Mercator, which over-reads by
          ~1.44&times; at Alberta latitudes &mdash; enough to turn a
          200&nbsp;m spacing into a fictional 290. Each phase writes
          one Parquet artifact, so every downstream step is
          re-runnable and diffable.</li>
        <li><strong>3&ndash;5 · Offsets &amp; timing.</strong>
          Candidate offset pairs come from a 2&nbsp;km spatial grid
          index rather than an O(N&sup2;) sweep; each survives as a
          vectorized minimum segment-to-segment closest approach
          over every leg pair, both directions (overlap is
          asymmetric), plus TVD offset, overlap fraction, and
          relative azimuth &mdash; with the same math run
          <em>within</em> each multilateral for the fishbone
          self-spacing signal. Sanity anchor: pad siblings should
          read ~200&ndash;400&nbsp;m apart at near-zero azimuth
          delta. Then the convention that keeps training honest:
          every feature describes the world <em>as of the
          well&rsquo;s own spud date</em> &mdash; only offsets
          already on production count as parents, depletion is
          cumulative withdrawal before spud, and a later well never
          rewrites an earlier well&rsquo;s row. Pace is normalized
          by a per-well EUR from an Arps decline fit, so the target
          is &ldquo;% of expected recovery per year,&rdquo; not raw
          barrels that reward long laterals.</li>
        <li><strong>6&ndash;7 · Dataset &amp; baselines first.</strong>
          One training example per well: its own static description
          (lateral length, leg count, fan spread, vintage, TVD,
          play), its set of legs, and its set of offsets. The
          anti-fooling design does the heavy lifting: pad-mates are
          never split across train and validation (they share rock
          &mdash; a random split lets the model memorize geology and
          call it skill), the newest wells are sealed for the blind
          lookback, and normalization statistics come from training
          wells only. Before any neural network exists, a baseline
          ladder is fitted: global median (the floor), ridge
          regression on own-well features (roughly the
          geology-and-design share), and gradient boosting with the
          offset set flattened into aggregates &mdash; the honest
          competitor. If boosting can&rsquo;t beat ridge, the offset
          signal isn&rsquo;t there and no amount of architecture
          will conjure it.</li>
        <li><strong>8&ndash;9 · Model &amp; the gate.</strong> The
          two set encoders with attention pooling &mdash; small by
          design, tens of thousands of parameters, trains on a
          laptop &mdash; output three monotone quantiles under
          pinball loss, the standard way to make a network forecast
          honest P10/P50/P90 bands rather than a single average.
          The empty-offset-list path (the counterfactual itself) is
          unit-tested to stay finite, along with
          permutation-invariance: shuffling the offset list must not
          change the forecast. Then the sealed wells are opened
          exactly once, and physics probes hold veto power over the
          MAE: isolated wells must price near zero, penalty must
          rise with depletion, thinning a fishbone&rsquo;s legs must
          relieve its self-interference.</li>
        <li><strong>10&ndash;11 · What-ifs &amp; the report.</strong>
          Score every well three ways: as drilled; offsets removed
          (the parent&ndash;child penalty); own legs thinned to a
          reference spacing (the fishbone penalty). Each score
          carries an in-domain flag &mdash; a well from a
          play-vintage combination the training data never saw gets
          its penalty marked untrustworthy rather than served
          anyway. And the report writes down everything, including
          the part that stung: the flattering single-run headline
          numbers shrank when training was repeated from different
          random starts and averaged, so the averaged numbers are
          the ones published here.</li>
      </ol>

      <h2>From playbook to well planner</h2>
      <p>
        The finish line is drawn honestly: an optimal-spacing
        prescription is <strong>not deliverable from public data
        alone</strong>. Three inputs that decide the answer are
        censored from the public record, and each maps to a specific
        failure you can see in the results above:
      </p>
      <ul class="gaps">
        <li><strong>Full production histories.</strong> Petrinex&rsquo;s
          free window is a rolling ~5 years, so a 2015 parent&rsquo;s
          early drawdown &mdash; the depletion that actually hits a
          2023 child &mdash; is invisible. The model prices depletion
          it can see; it can&rsquo;t price what the window censors.</li>
        <li><strong>Completion design.</strong> Tonnage, stages,
          fluid, intensity &mdash; none of it is in the public
          Alberta record. This is precisely why the model reads
          crowding through depletion rather than geometry: without
          frac data it cannot distinguish a tightly-spaced pad from
          a frac-hit waiting to happen. The <code>approach FAIL</code>
          above is this gap, measured.</li>
        <li><strong>Within-pad geology.</strong> Tops, logs, mapped
          reservoir properties. The within-pad trick removes geology
          by holding it fixed; with actual geological data the model
          could <em>use</em> it instead of controlling for it.</li>
      </ul>
      <p>
        Here is the part worth saying plainly: <strong>none of these
        are exotic.</strong> They are standard tables in commercial
        well-data subscriptions &mdash; the kind of catalog a vendor
        like <a href="https://www.geologic.com/">geoLOGIC</a> has
        maintained for Western Canada for decades: complete
        production back to first oil, completion and frac detail,
        interpreted tops. This playbook&rsquo;s gap list reads like
        a page from that catalog, and that is not a coincidence.
        Load those three inputs into the pipeline exactly as built
        &mdash; they slot into the same offset descriptions the set
        encoders already consume &mdash; retrain, and the cautionary
        tale becomes a <strong>working ML well planner</strong>:
        calibrated P10/P50/P90 for a proposed location, the
        parent&ndash;child penalty priced <em>before</em> permitting,
        spacing sensitivity run as a batch of what-ifs instead of a
        multi-well science experiment. <a
        href="https://github.com/rbhughes/well-spacing-playbook/blob/main/docs/WITH_PROPRIETARY_DATA.md">
        <code>WITH_PROPRIETARY_DATA.md</code></a> is the drop-in
        spec, gap by gap. The machinery is finished; it is waiting
        on the data.
      </p>
    </article>

    <footer>
      <p>
        Data: AER ST37 directional surveys and Petrinex volumetrics
        (Government of Alberta; re-fetched, never re-hosted).
        Full chain of evidence:
        <a href="https://github.com/rbhughes/well-spacing-playbook/blob/main/reports/interference_report.md">reports/interference_report.md</a>.
        Basemap tiles by <a href="https://openfreemap.org">OpenFreeMap</a>.
        Non-commercial demonstration; derived statistics only.
      </p>
    </footer>

    <script src="../scripts/app.js"></script>
  </body>
</html>

<style is:global>
  :root {
    --page: #eeeeeb;
    --surface: #fcfcfb;
    --ink: #0b0b0b;
    --ink-2: #52514e;
    --muted: #898781;
    --hairline: #ddddd6;
    --border: rgba(11, 11, 11, 0.1);
    --accent: #256abf;
    --brand: #d95f00;
  }
  * { box-sizing: border-box; }
  body {
    margin: 0;
    background: var(--page);
    color: var(--ink);
    font: 15px/1.5 system-ui, -apple-system, "Segoe UI", sans-serif;
  }
  a { color: var(--accent); }
  .topnav {
    display: flex; align-items: center; gap: 0.5rem;
    padding: 0.55rem 2.1rem;
    border-bottom: 1px solid var(--hairline);
    background: var(--surface);
  }
  .topnav .logo img { height: 26px; width: 26px; display: block; border-radius: 6px; }
  .topnav .brand { font-weight: 700; color: var(--brand); margin-right: 0.5rem; }
  .topnav a { color: var(--brand); font-weight: 700; text-decoration: none; font-size: 0.92rem; }
  .topnav .vintage { margin-left: auto; color: var(--muted); font-size: 0.85rem; }
  .masthead {
    max-width: 62rem; margin: 0 auto;
    padding: 1.6rem 2.1rem 0.4rem;
    text-align: center;
    border-bottom: 3px double var(--hairline);
  }
  .kicker { color: var(--muted); letter-spacing: 0.18em; font-size: 0.75rem; margin: 0; }
  h1 {
    margin: 0.4rem 0;
    font: 700 2.3rem/1.15 Georgia, "Iowan Old Style", serif;
  }
  .standfirst {
    font: 1.2rem/1.6 Georgia, "Iowan Old Style", serif;
    color: var(--ink-2);
    margin: 0.6rem auto 1.2rem;
    max-width: 46rem;
  }
  .body { max-width: 62rem; margin: 0 auto; padding: 0.8rem 2.1rem; }
  .body h2 { font: 700 1.35rem/1.3 Georgia, serif; margin: 1.6rem 0 0.5rem; }
  .body p, .body li {
    font: 1.12rem/1.65 Georgia, "Iowan Old Style", serif;
  }
  .cards { display: flex; gap: 0.9rem; flex-wrap: wrap; margin: 1rem 0; }
  .card {
    flex: 1; min-width: 240px;
    background: var(--surface);
    border: 1.5px solid var(--hairline);
    border-radius: 12px;
    padding: 0.9rem 1.1rem;
  }
  .card .num { font: 700 2.4rem/1 Georgia, serif; margin-bottom: 0.4rem; }
  .num.confound { color: #898781; }
  .num.true-sign { color: #d7301f; }
  .num.penalty { color: var(--brand); }
  .card p { font-size: 0.98rem; line-height: 1.5; margin: 0; }
  figure { margin: 1rem 0; }
  figure svg {
    width: 100%; height: auto;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 10px;
  }
  figcaption { color: var(--muted); font: 0.85rem/1.4 system-ui, sans-serif; margin-top: 0.35rem; }
  figcaption code, .body code {
    font-family: ui-monospace, Menlo, monospace;
    font-size: 0.86em;
    background: var(--surface);
    border: 1px solid var(--hairline);
    border-radius: 4px;
    padding: 0.05em 0.3em;
  }
  .map-box {
    height: 64vh; min-height: 440px;
    border-radius: 12px;
    border: 1px solid var(--border);
    background: var(--surface);
    margin: 1rem 0 0.4rem;
  }
  .fish-box {
    height: 440px;
    border-radius: 12px;
    border: 1px solid var(--border);
    background: var(--surface);
    overflow: hidden;
    cursor: grab;
    touch-action: pan-y;
  }
  .fish-box canvas { display: block; width: 100%; height: 100%; }
  .mapnote { color: var(--ink-2); font-size: 1rem !important; }
  .phases { padding-left: 1.2rem; }
  .phases li { margin: 0.5rem 0; }
  .gaps { padding-left: 1.2rem; }
  .gaps li { margin: 0.5rem 0; }
  footer {
    color: var(--muted); font-size: 0.8rem;
    max-width: 62rem; margin: 0 auto;
    padding: 1rem 2.1rem 1.4rem;
    border-top: 1px solid var(--hairline);
  }
  @media (max-width: 700px) {
    h1 { font-size: 1.7rem; }
    .masthead, .body, footer { padding-left: 1.2rem; padding-right: 1.2rem; }
  }
</style>
